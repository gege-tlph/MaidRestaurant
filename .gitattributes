# Auto detect text files and perform LF normalization
* text=auto

# Workflow files and the scripts they run must stay LF for the Linux runners.
*.yml text eol=lf
*.yaml text eol=lf
*.py text eol=lf
*.sh text eol=lf
